# Algorithm-learning
## 冒泡排序
### 原理

冒泡排序将大的数据像水中的气泡一样逐渐“浮”到后面，从而实现排序（由小到大）

### 实现

bubble.c中的bubble()函数实现冒泡排序，通过两层for循环，第一层for循环从前往后逐一选定元素，第二层循环从选定的元素开始逐一向后比较，若顺序不符合要求则交换两元素，从而实现“冒泡”的效果

### 复杂度

时间复杂度：最好情况（完全有序）O(n)   最坏情况（完全倒序）O(n^2)   平均情况O(n^2)
